# Pour la plupart des projets, ce fichier de workflow ne nécessitera pas de modifications ;
# il suffit de le committer dans votre dépôt.
#
# Vous pourriez souhaiter modifier ce fichier pour remplacer l'ensemble des langages analysés,
# ou pour fournir des requêtes ou une logique de construction personnalisées.
#
# ******** NOTE ********
# Nous avons tenté de détecter les langages utilisés dans votre dépôt. Veuillez vérifier
# la matrice `language` définie ci-dessous pour confirmer que vous avez l'ensemble correct
# de langages CodeQL pris en charge.
#
name: "CodeQL Avancé"

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  schedule:
    - cron: '37 20 * * 3' # Ce workflow est planifié pour s'exécuter chaque mercredi à 20h37

jobs:
  analyze:
    name: Analyse (${{ matrix.language }})
    # La taille du runner impacte le temps d'analyse CodeQL. Pour en savoir plus, voir :
    #   - https://gh.io/recommended-hardware-resources-for-running-codeql
    #   - https://gh.io/supported-runners-and-hardware-resources
    #   - https://gh.io/using-larger-runners (GitHub.com uniquement)
    # Envisagez d'utiliser des runners plus puissants ou des machines avec plus de ressources pour améliorer les temps d'analyse.
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    permissions:
      # requis pour tous les workflows
      security-events: write

      # requis pour récupérer des packs CodeQL internes ou privés
      packages: read

      # requis uniquement pour les workflows dans des dépôts privés
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: javascript-typescript
            build-mode: none
        # CodeQL prend en charge les mots-clés de valeurs suivants pour 'language' : 'c-cpp', 'csharp', 'go', 'java-kotlin', 'javascript-typescript', 'python', 'ruby', 'swift'
        # Utilisez `c-cpp` pour analyser du code écrit en C, C++ ou les deux.
        # Utilisez 'java-kotlin' pour analyser du code écrit en Java, Kotlin ou les deux.
        # Utilisez 'javascript-typescript' pour analyser du code écrit en JavaScript, TypeScript ou les deux.
        # Pour en savoir plus sur la modification des langages analysés ou la personnalisation du mode de build pour votre analyse,
        # consultez https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/customizing-your-advanced-setup-for-code-scanning.
        # Si vous analysez un langage compilé, vous pouvez modifier le 'build-mode' pour ce langage afin de personnaliser la façon dont
        # votre code est analysé, voir https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/codeql-code-scanning-for-compiled-languages
    steps:
      - name: Récupérer le dépôt
        uses: actions/checkout@v4

      # Initialise les outils CodeQL pour l'analyse.
      - name: Initialiser CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          # Si vous souhaitez spécifier des requêtes personnalisées, vous pouvez le faire ici ou dans un fichier de configuration.
          # Par défaut, les requêtes listées ici remplaceront celles spécifiées dans un fichier de configuration.
          # Préfixez la liste ici avec "+" pour utiliser ces requêtes et celles du fichier de configuration.

          # Pour plus de détails sur les packs de requêtes CodeQL, consultez : https://docs.github.com/en/code-security/code-scanning/automatically-scanning-your-code-for-vulnerabilities-and-errors/configuring-code-scanning#using-queries-in-ql-packs
          # queries: security-extended,security-and-quality

      # Si l'étape d'analyse échoue pour l'un des langages que vous analysez avec
      # "Nous n'avons pas pu automatiquement construire votre code", modifiez la matrice ci-dessus
      # pour définir le mode de build sur "manual" pour ce langage. Ensuite, modifiez cette étape
      # pour construire votre code.
      # ℹ️ Programmes en ligne de commande à exécuter via le shell de l'OS.
      # 📚 Voir https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idstepsrun
      - if: matrix.build-mode == 'manual'
        shell: bash
        run: |
          echo 'Si vous utilisez un mode de build "manual" pour un ou plusieurs des' \
            'langages que vous analysez, remplacez cela par les commandes pour construire' \
            'votre code, par exemple :'
          echo '  make bootstrap'
          echo '  make release'
          exit 1

      - name: Effectuer l'analyse CodeQL
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{matrix.language}}"
